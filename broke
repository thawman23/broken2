from flask import Flask, jsonify

app = Flask(__name__)

users = {
    1: {"name": "Bharadwaj"},
    2: {"name": "Teja"}
}

@app.route("/users/<int:user_id>")
def get_user(user_id):

    user = users[user_id]

    return jsonify(user)

if __name__ == "__main__":
    app.run()
